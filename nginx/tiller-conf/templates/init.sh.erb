#!/bin/bash

<%if not endpoints.select{|e|e['name'] and e['port'] == 443}.empty?%>
echo "Starting init nginx..."
nginx &
echo "Waiting ELB..."
sleep 15
/letsencrypt/letsencrypt-auto auth -c /letsencrypt.ini
nginx -s stop
<%end%>
usr/local/bin/tiller -e prod -x nginx
